// Data initialization page
import 'package:flutter/material.dart';

class DataInitializationPage extends StatelessWidget {
  const DataInitializationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Data Initialization')),
      body: const Center(child: Text('Data Initialization Page')),
    );
  }
}
